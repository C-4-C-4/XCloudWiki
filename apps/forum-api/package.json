{
  "name": "forum-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute forum-db --local --file=./src/db/schema.sql",
    "db:init:remote": "wrangler d1 execute forum-db --remote --file=./src/db/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.22.0"
  }
}
